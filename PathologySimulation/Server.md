# 连接服务器
1. 服务器配置
```
sudo apt install ssh
systemctl status ssh
```

2. 本机PC配置
Vscode获取 SSH扩展
```
ssh dell@10.201.23.23
```

# 配置CODEX
## 须知
- 在本机 VS Code 打开 SSH 终端，Codex不一定能读
    你：开了一个远程终端，看得到服务器
    Codex：在本地 VS Code 里，看的是本地项目和本地环境




